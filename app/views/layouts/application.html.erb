<!DOCTYPE html>
<html>
  <head>
    <title>EnjoyMusic</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    <header>
      <div>
        <h1>enjoy music!<h1>

        <% if user_signed_in? %>
          <li>
            <%= link_to "New Song", destroy_user_session_path, method: :delete %>
          </li>
          <li>
            <%= link_to "My Page", destroy_user_session_path, method: :delete %>
          </li>
          <li>
            <%= link_to "Songs", destroy_user_session_path, method: :delete %>
          </li>
          <li>
            <%= link_to "Sign Out", destroy_user_session_path, method: :delete %>
          </li>
        <% else %>
          <li>
            <%= link_to "Sign Up", new_user_registration_path %>
          </li>
          <li>
            <%= link_to "Sign in", new_user_session_path %>
          </li>
          <li>
            <%= link_to "Songs", destroy_user_session_path, method: :delete %>
          </li>
          <li>
            <%= link_to "About", destroy_user_session_path, method: :delete %>
          </li>
        <% end %>

      </div>
    </header>

    <%= yield %>

    <footer>
      <p><%= link_to "contact" %></p>
    </footer>
  </body>
</html>
